<UserControl x:Class="ApiManager.Views.RunDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ApiManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<UserControl.Resources>
		<ImageBrush x:Key="ApiButtonImageBrush" ImageSource="../images/api.png" />
		<ImageBrush x:Key="ExtractVariableButtonImageBrush" ImageSource="../images/extract.png" />
		<ImageBrush x:Key="AssertSuccessButtonImageBrush" ImageSource="../images/assert_success.png" />
		<ImageBrush x:Key="AssertFailButtonImageBrush" ImageSource="../images/assert_fail.png" />
		
		<Style x:Key="DataHeaderStyle" TargetType="{x:Type GridViewColumnHeader}">
			<Setter Property="Visibility" Value="Collapsed" />
		</Style>

		<Style x:Key="ExtractVariableStyle">
			<Setter Property="Control.Background" Value="Red" />
			<Setter Property="Control.FontWeight" Value="Bold" />
			<Style.Triggers>
				<DataTrigger Binding="{Binding ExtractVariableInfo.Success}" Value="True" >
					<Setter Property="Control.Background" Value="Green" />
				</DataTrigger>
			</Style.Triggers>
		</Style>

		<Style x:Key="AssertStyle">
			<Setter Property="Control.Background" Value="Red" />
			<Setter Property="Control.FontWeight" Value="Bold" />
			<Style.Triggers>
				<DataTrigger Binding="{Binding AssertInfo.Success}" Value="True" >
					<Setter Property="Control.Background" Value="Green" />
				</DataTrigger>
			</Style.Triggers>
		</Style>

		<Style x:Key="AssertButtonStyle">
			<Setter Property="Control.Background" Value="{StaticResource AssertFailButtonImageBrush}" />
			<Style.Triggers>
				<DataTrigger Binding="{Binding AssertInfo.Success}" Value="True" >
					<Setter Property="Control.Background" Value="{StaticResource AssertSuccessButtonImageBrush}" />
				</DataTrigger>
			</Style.Triggers>
		</Style>

		<Style x:Key="ApiStyle" >
			<Setter Property="Control.Foreground" Value="Red" />
			<Setter Property="Control.FontWeight" Value="Bold" />
			<Style.Triggers>
				<DataTrigger Binding="{Binding ApiInfo.StatusCode}" Value="OK" >
					<Setter Property="Control.Foreground" Value="Green" />
				</DataTrigger>
				<DataTrigger Binding="{Binding ApiInfo.StatusCode}" Value="Created" >
					<Setter Property="Control.Foreground" Value="Green" />
				</DataTrigger>
			</Style.Triggers>
		</Style>

		<Style x:Key="ClearButtonStyle">
			<Setter Property="Control.Visibility" Value="Visible" />
			<Style.Triggers>
				<DataTrigger Binding="{Binding ApisCount}" Value="0" >
					<Setter Property="Control.Visibility" Value="Hidden" />
				</DataTrigger>
			</Style.Triggers>
		</Style>
		
		<Style x:Key="InfoTextBoxStyle">
			<Setter Property="Control.Visibility" Value="Hidden" />
			<Style.Triggers>
				<DataTrigger Binding="{Binding ApisCount}" Value="0" >
					<Setter Property="Control.Visibility" Value="Visible" />
				</DataTrigger>
			</Style.Triggers>
		</Style>

		<Style x:Key="ApplicationLinkButtonNoUnderLine" TargetType="{x:Type Button}">
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type Button}">
						<TextBlock>
						    <ContentPresenter />
						</TextBlock>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
			<Setter Property="HorizontalContentAlignment" Value="Center"/>
			<Setter Property="VerticalContentAlignment" Value="Center"/>
			<Setter Property="Foreground" Value="Blue" />
			<Setter Property="FontSize" Value="13" />
			<Setter Property="FontWeight" Value="Bold" />
			<Setter Property="Margin" Value="15,0,0,0" />
			<Setter Property="Cursor" Value="Hand" />

			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="true">
					<Setter Property="Foreground" Value="Red" />
				</Trigger>
				<DataTrigger Binding="{Binding ApiInfo.JwtToken}" Value="">
					<Setter Property="Visibility" Value="Hidden" />
				</DataTrigger>
			</Style.Triggers>
		</Style>

		<DataTemplate x:Key="AssertHeaderTemplate">
			<Expander >
				<Expander.Header>
					<StackPanel Orientation="Horizontal" Style="{StaticResource AssertStyle}">
						<Button  Style="{StaticResource AssertButtonStyle}" Width="22" Height="22"/>
						<TextBlock  Text="{Binding AssertInfo.Variable}" Margin="3,0,0,0" Width="80" Foreground="Black" HorizontalAlignment="Right" FontWeight="Bold"/>
						<TextBlock  Text="{Binding AssertInfo.Expected}" Margin="3,0,0,0" Width="80" Foreground="Black" HorizontalAlignment="Right" FontWeight="Bold"/>
						<TextBlock Text=" = " FontWeight="Bold" Foreground="Black" />
						<TextBlock  Text="{Binding AssertInfo.Actual}" Margin="3,0,0,0" Width="80" Foreground="Black" HorizontalAlignment="Right" FontWeight="Bold"/>
					</StackPanel>
				</Expander.Header>
				<Grid Background="White">
					<Grid.RowDefinitions>
						<RowDefinition Height="100"/>
					</Grid.RowDefinitions>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="700"/>
					</Grid.ColumnDefinitions>
					<TextBox Grid.Row="0" Grid.Column="0" Text="{Binding AssertInfo.Message}" TextWrapping="Wrap" Height="400" Foreground="Black" />
				</Grid>
			</Expander>
		</DataTemplate>

		<DataTemplate x:Key="ExtractHeaderTemplate">
			<Expander >
				<Expander.Header>
					<StackPanel Orientation="Horizontal" Style="{StaticResource ExtractVariableStyle}">
						<Button  Background="{StaticResource ExtractVariableButtonImageBrush}" Width="22" Height="22"/>
						<TextBlock  Text="{Binding ExtractVariableInfo.Variable}" Margin="3,0,0,0" Width="80" Foreground="Black" HorizontalAlignment="Right" FontWeight="Bold"/>
						<TextBlock Text=" = " FontWeight="Bold" Foreground="Black" />
						<TextBlock  Text="{Binding ExtractVariableInfo.ShortValue}" Margin="3,0,0,0" Foreground="Black" FontWeight="Bold"/>
					</StackPanel>
				</Expander.Header>
				<Grid Background="White">
					<Grid.RowDefinitions>
						<RowDefinition Height="30"/>
						<RowDefinition Height="300"/>
					</Grid.RowDefinitions>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="700"/>
					</Grid.ColumnDefinitions>
					<TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding ExtractVariableInfo.Variable}" FontWeight="Bold"/>
					<TextBox Grid.Row="1" Grid.Column="0" Text="{Binding ExtractVariableInfo.ValueOrMessage}" TextWrapping="Wrap" Height="400" Foreground="Black" />
				</Grid>
			</Expander>
		</DataTemplate>

		<DataTemplate x:Key="ApiHeaderTemplate">
			<Expander Background="White">
				<Expander.Header>
					<StackPanel Orientation="Horizontal" Style="{StaticResource ApiStyle}"  Background="White">
						<Button  Background="{StaticResource ApiButtonImageBrush}" Width="22" Height="22"/>
						<TextBlock  Text="{Binding ApiInfo.Method}" Margin="3,0,0,0" Width="40"/>
						<TextBlock  Text="{Binding ApiInfo.RelativeUrl}" Margin="3,0,0,0" Width="140"/>
						<TextBlock  Text="{Binding ApiInfo.StatusCode}" Margin="3,0,0,0" Width="60"/>
						<StackPanel Orientation="Horizontal" Margin="3,0,0,0">
							<TextBlock  Text="{Binding ApiInfo.TimeTakenString}" HorizontalAlignment="Right"/>
							<TextBlock Text="(msec)" HorizontalAlignment="Left"/>
						</StackPanel>
						<Button Content="JwtToken" Style="{StaticResource ApplicationLinkButtonNoUnderLine}" Command="{Binding ShowJwtTokenCommand}"/>
					</StackPanel>
				</Expander.Header>
				<local:RequestResponseContainerView  DataContext="{Binding ApiInfo}" Height="500"/>
			</Expander>
		</DataTemplate>

		<local:InfoDataHeaderTemplateSelector x:Key="InfoDataHeaderTemplateSelector" 
                                             ApiInfoHeaderTemplate="{StaticResource ApiHeaderTemplate}"
                                             ExtractHeaderTemplate="{StaticResource ExtractHeaderTemplate}"
											  AssertHeaderTemplate="{StaticResource AssertHeaderTemplate}"
											  
											  
                                        />

		<!--<local:InfoDataTemplateSelector x:Key="InfoDataTemplateSelector" 
                                             ApiDataTemplate="{StaticResource ApiDataTemplate}"
                                             ExtractDataTemplate="{StaticResource ExtractDataTemplate}"
                                        />-->

	</UserControl.Resources>

	<Grid>
		<Grid Margin="0,20,0,0">
			<StackPanel Background="White">
				<TextBlock Text="Api Responses will be shown here." FontWeight="Bold" HorizontalAlignment="Left" Style="{StaticResource InfoTextBoxStyle}" />
				<Button Content="Clear" Command="{Binding ClearCommand}" Width="60" Height="25" Margin="3,0,0,0" HorizontalAlignment="Left" Style="{StaticResource ClearButtonStyle}"/>
				<ListView ItemsSource="{Binding ApiInfos}" BorderThickness="0" ItemTemplateSelector="{StaticResource InfoDataHeaderTemplateSelector}" Background="White" />
			</StackPanel>
		</Grid>

	</Grid>
</UserControl>
