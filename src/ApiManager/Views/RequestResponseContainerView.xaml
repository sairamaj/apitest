<UserControl x:Class="ApiManager.Views.RequestResponseContainerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ApiManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="40"/>
			<RowDefinition />
			<RowDefinition Height="3"/>
			<RowDefinition />
		</Grid.RowDefinitions>

		<StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left">
			<Button Height="35" Width="100" FontWeight="Bold" Margin="10,0,0,0" 
					Content="Try Again" Command="{Binding SubmitRequestCommand}" HorizontalAlignment="Left"
					VerticalContentAlignment="Center"
					/>
			<TextBlock Text="(You can change the request and submit just this request alone.)" 
					   VerticalAlignment="Center"
					   Margin="4,0,0,0" />
		</StackPanel>
		<local:RequestView Grid.Row="1" Grid.Column="0" DataContext="{Binding ApiInfo.Request}"/>
		<GridSplitter Grid.Row="2" Grid.Column="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  />
		<local:ResponseView Grid.Row="3" Grid.Column="0" DataContext="{Binding ApiInfo.Response}"/>
	</Grid>
</UserControl>
