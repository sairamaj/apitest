<UserControl x:Class="ApiManager.Views.CommandsFileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ApiManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<UserControl.Resources>
		<Style x:Key="DataHeaderStyle" TargetType="{x:Type GridViewColumnHeader}">
			<Setter Property="Visibility" Value="Collapsed" />
		</Style>
	</UserControl.Resources>
	<Grid >
		<StackPanel>
			<TextBlock Text="Command Files:" FontWeight="Bold"/>
			<ListView ItemsSource="{Binding CommandFiles}" BorderThickness="0" Margin="10,0,0,0" SelectedItem="{Binding SelectedCommandFile}">
				<ListView.View >
					<GridView ColumnHeaderContainerStyle="{StaticResource DataHeaderStyle}">
						<GridViewColumn  DisplayMemberBinding="{Binding}">
							<GridViewColumnHeader Tag="Name" Content="Name"/>
						</GridViewColumn>
					</GridView>
				</ListView.View>
			</ListView>
		</StackPanel>
	</Grid>
</UserControl>
