<UserControl x:Class="ApiManager.QuickTest.Views.HttpRequestEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ApiManager.QuickTest.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
		<StackPanel Orientation="Horizontal" Height="30">
			<ComboBox ItemsSource="{Binding Methods}" SelectedItem="{Binding HttpMethod}" Width="80" Height="25"/>
			<TextBox Text="{Binding Url}" Width="350" Margin="2,0,0,0" Height="25"/>
			<Button Content="Send" Margin="2,0,0,0" Height="25" Command="{Binding SendCommand}" />
		</StackPanel>
    </Grid>
</UserControl>
